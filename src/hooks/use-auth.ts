// Legacy compatibility layer - Re-export from centralized auth context
// This maintains backward compatibility while using the optimized auth system

export { useAuth } from '@/contexts/auth-context'

